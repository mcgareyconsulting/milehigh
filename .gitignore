venv
.venv
.env
.env.*
instance/
.vscode/

# Node artifacts anywhere in the tree. frontend/.gitignore already covers the
# real one; this catches a stray npm/npx run from the repo root, which leaves a
# node_modules/.vite cache next to a package-lock.json that has no package.json.
node_modules/

# Test / coverage artifacts
.pytest_cache/
.coverage
.coverage.*
coverage.xml
htmlcov/
test-results.xml

#Codex
.agents/
.codex/
AGENTS.md

# Log files
logs/
*.log

# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# IDE files
.idea/
*.swp
*.swo

# OS files
.DS_Store
Thumbs.db

# Temporary files
*.tmp
*.temp

# Snapshot files
excel_snapshots/

# PDF markup uploads (per-release drawing versions; ephemeral on Render)
app/storage/

# Refresh staging db script
refresh_staging_db.sh
prod.dump

# DannyOS roadmap build artifact (derived from docs/ROADMAP.md)
roadmap.json
