# Testing dependencies for Trello-OneDrive sync application

# Core testing framework
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.11.0
pytest-xdist>=3.3.0  # For parallel test execution
pytest-html>=3.2.0   # For HTML test reports

# Test utilities
factory-boy>=3.3.0   # For creating test data
freezegun>=1.2.0     # For mocking datetime
responses>=0.23.0    # For mocking HTTP requests
pytest-flask>=1.2.0  # Flask testing utilities

# Coverage and quality
coverage>=7.3.0
pytest-clarity>=1.0.0  # Better test failure output

# Performance testing (optional)
pytest-benchmark>=4.0.0

# Database testing
pytest-postgresql>=5.0.0  # For PostgreSQL testing (if needed)

# Async testing (if needed in future)
pytest-asyncio>=0.21.0
