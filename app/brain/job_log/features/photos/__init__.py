"""Release photo attachments (job-site / progress images).

A flat (non-versioned) list of images attached to a release, each with an
optional editable note. Distinct from the versioned PDF drawings in the
sibling `pdf_markup` feature.
"""
