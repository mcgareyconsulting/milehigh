"""Release Issue & Error Register (roadmap T11).

Per-release issue records with their own comments (@mentions), photo/PDF
attachments and field-level change history. Admin-only in v1; departments are a
label only (no routing), assignment and Safety Hold are deferred.
"""
