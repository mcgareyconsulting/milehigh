"""Microsoft Graph integration (app-only / client-credentials).

Holds the shared Graph client used to read the dedicated Banana Boy mailbox
(bb@mhmw.com) as an application — the workaround for not opening Graph across
the whole org. See graph_app_client.py.
"""
