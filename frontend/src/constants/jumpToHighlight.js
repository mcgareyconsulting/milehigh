/** CSS class for yellow row highlight when jumping from Job Search (applies to td children; box-shadow on tr doesn't work in WebKit) */
export const JUMP_TO_HIGHLIGHT_CLASS = 'jump-to-highlight-row';
